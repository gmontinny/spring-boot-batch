apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gmontinny

resources:
  - namespace.yaml
  - rbac.yaml
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
  - pdb.yaml

commonLabels:
  app.kubernetes.io/name: gmontinny
  app.kubernetes.io/managed-by: kustomize
